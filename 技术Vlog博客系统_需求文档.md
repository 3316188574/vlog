# 技术 Vlog 博客系统——需求文档（标准版）

版本：v0.1（草案）  
日期：2026-06-02  
作者：SOLO（项目架构支持）

---

## 1. 项目背景与目标

### 1.1 背景
为沉淀与分享技术内容（文章 / Vlog 相关图文），需要一个可公开访问的技术博客系统，并提供后台管理能力以便持续发布、维护内容。系统面向个人或小团队使用，优先追求开发效率、可维护性与可扩展性。

### 1.2 目标
1. **对外展示**：提供公开前台，用于浏览文章列表、阅读文章详情、按时间/分类筛选。
2. **内容管理**：提供需要登录的后台，实现文章的新增、编辑、删除，并支持草稿/发布流程。
3. **互动与媒体**：支持评论功能与图片上传能力（用于文章内容或封面等）。
4. **可持续迭代**：架构清晰，便于后续扩展（如全文检索、点赞、订阅、更多角色权限）。

### 1.3 范围说明（In Scope / Out of Scope）
**In Scope（本期范围）**
- 文章发布与展示（含分类/标签、筛选、草稿/发布）
- 登录后台与内容管理
- 评论（基础功能）
- 图片上传（基础功能）

**Out of Scope（本期不做/可后续迭代）**
- 多管理员/复杂 RBAC 权限体系（本期默认单一“后台管理员”）
- 审核流、内容推荐算法、订阅推送
- 分布式部署、高并发大规模访问（本期以中小访问量为目标）

---

## 2. 用户角色

### 2.1 访客（前台用户）
- 未登录用户
- 目标：浏览文章、筛选内容、查看评论、发表/管理自己的评论（本期可简化为“匿名评论”或“昵称+邮箱评论”，见 3.1.4 验收项）

### 2.2 后台管理员
- 需要登录
- 目标：管理文章（新增、编辑、删除、发布/草稿）、管理分类/标签（至少可在文章编辑时选择/输入）、管理评论（删除/隐藏）

---

## 3. 功能需求
说明：每条需求包含**验收标准**（可测试/可验收）。

### 3.1 前台（公开访问）

#### 3.1.1 文章列表页
**需求点**
1. 展示文章列表：至少包含标题、摘要（可选）、发布时间、分类、标签、封面图（可选）。
2. 支持分页加载（页码或“加载更多”二选一，建议页码）。
3. 列表默认按发布时间倒序。

**验收标准**
- 访问首页或列表页可看到文章列表；每条记录至少展示标题与发布时间。
- 当文章数量超过单页限制（如 10 条）时，可通过分页切换查看其他页面。
- 列表顺序符合发布时间倒序。

#### 3.1.2 文章筛选与归档
**需求点**
1. 支持按时间筛选：按年月归档或时间范围筛选（建议按年月归档）。
2. 支持按分类筛选（用户提出：前台做分类筛选）。
3. 支持按标签筛选（用户提出：数据表增加 tags 字段/或标签表）。

**验收标准**
- 选择某分类后，列表仅显示该分类文章，且筛选条件在 UI 上可见（如面包屑/筛选标签）。
- 选择某标签后，列表仅显示包含该标签的文章。
- 选择某年月（或范围）后，列表仅显示对应时间范围文章。

#### 3.1.3 文章详情页
**需求点**
1. 展示文章完整内容（支持富文本/Markdown 渲染，建议 Markdown）。
2. 展示文章元信息：发布时间、分类、标签。
3. 支持上一篇/下一篇导航（可选但推荐）。
4. 支持图片在内容中正确显示（配合图片上传功能）。

**验收标准**
- 点击列表任意文章进入详情页，内容与后台保存一致，排版无明显错乱。
- 文章内图片可正常显示（不出现 404/跨域问题）。

#### 3.1.4 评论功能（前台）
**需求点（用户提出：需要评论功能）**
1. 文章详情页展示评论列表（按时间正序/倒序二选一，建议倒序）。
2. 支持发表评论：
   - 方案 A（推荐，轻量）：匿名评论（昵称 + 可选邮箱）+ 内容；
   - 方案 B：登录后评论（需要额外的访客账号体系，本期不推荐）。
3. 支持基础反垃圾：最小提交间隔、长度限制、简单敏感词/链接限制（可后置）。

**验收标准**
- 在文章详情页可看到评论列表；无评论时显示“暂无评论”提示。
- 提交评论后在不刷新或刷新后都能看到新增评论。
- 评论字段校验生效（如内容不能为空、长度上限）。

#### 3.1.5 错误与空态
**需求点**
1. 文章不存在：返回 404 页面/提示。
2. 无数据时：列表页/筛选结果提供清晰空态提示。

**验收标准**
- 访问不存在文章 ID 的详情页，展示 404 或“文章不存在”提示，并不会导致前端白屏。

---

### 3.2 后台（需登录）

#### 3.2.1 登录与退出
**需求点**
1. 后台入口需要登录；未登录访问后台路由自动跳转登录页。
2. 登录成功后进入后台首页/文章管理列表。
3. 支持退出登录。

**验收标准**
- 未登录访问后台任意页面会跳转到登录页。
- 输入正确账号密码可登录；错误密码有明确错误提示。
- 退出后再次访问后台需重新登录。

#### 3.2.2 文章管理（列表）
**需求点**
1. 展示文章列表：标题、状态（draft/published）、分类、标签、更新时间/发布时间等。
2. 支持按状态筛选（用户提出：草稿功能，区分显示）。
3. 支持按分类/标签筛选（与前台一致的维度，便于管理）。

**验收标准**
- 后台列表可区分显示草稿与已发布文章（例如用状态列或标签）。
- 选择“草稿/已发布”筛选后列表结果正确。

#### 3.2.3 新增文章
**需求点**
1. 支持创建文章：标题、内容（Markdown）、分类、标签、封面图（可选）、摘要（可选）、状态（draft/published）。
2. 支持“保存为草稿”和“发布”两种操作。
3. 支持图片上传并插入正文（用户提出：需要图片上传）。

**验收标准**
- 新建文章保存为草稿后，前台不可见，后台可见且状态为 draft。
- 将文章发布后，前台列表可见且详情页可正常访问。
- 上传图片后返回可访问 URL，插入正文后前台可正常显示。

#### 3.2.4 编辑文章
**需求点**
1. 支持编辑文章全部字段。
2. 支持草稿 ↔ 发布状态切换（含发布时间逻辑，见 4.1 数据字段建议）。

**验收标准**
- 修改标题/内容/分类/标签后保存，前台（如已发布）展示内容同步更新。
- 草稿切换为发布后前台可见；发布切回草稿后前台不可见（或标记为未发布）。

#### 3.2.5 删除文章
**需求点**
1. 支持删除文章（建议软删除，便于恢复；也可先做硬删除）。
2. 删除前需二次确认。

**验收标准**
- 删除后后台列表不再显示该文章；前台不可访问。
- 删除操作需要确认弹窗，避免误删。

#### 3.2.6 分类与标签管理（最低可用）
**需求点**
1. 分类：
   - 最低可用：在文章编辑时可选择已有分类或新建分类；
   - 可选增强：提供独立的分类管理页（新增/编辑/删除/排序）。
2. 标签：
   - 最低可用：在文章编辑时可输入多个标签（逗号分隔/回车分词）并复用；
   - 可选增强：提供独立的标签管理页。

**验收标准**
- 新建分类后可在文章编辑中选择该分类，前台可按该分类筛选。
- 输入标签后文章保存成功，前台详情页展示标签且可点击筛选。

#### 3.2.7 评论管理（后台）
**需求点**
1. 查看评论列表：按文章/时间筛选。
2. 支持删除评论或设置为隐藏（推荐软删除/隐藏）。

**验收标准**
- 后台可看到某文章的评论列表。
- 删除/隐藏后前台评论列表不再展示对应评论。

#### 3.2.8 图片资源管理（可选增强）
**需求点**
1. 可查看已上传图片列表（按时间/引用文章）。
2. 支持删除未引用图片（谨慎，防止正文断链）。

**验收标准**
- 上传图片后在后台可查看资源记录（若实现该增强项）。

---

## 4. 非功能需求

### 4.1 性能
1. 前台列表页接口响应时间：在本地/单机环境下 **P95 ≤ 300ms**（不含网络延迟）。
2. 文章详情接口响应时间：**P95 ≤ 300ms**。
3. 支持分页查询，避免一次性拉取全量文章。

### 4.2 安全
1. 后台鉴权：
   - 使用 Token（如 JWT）或基于 Session 的方式实现登录态；
   - Token 需过期机制与刷新策略（可简化为短期 token + 重新登录）。
2. 密码安全：
   - 后台管理员密码使用安全哈希（bcrypt/argon2）存储；
   - 禁止明文存储。
3. 输入校验与防注入：
   - 后端使用 ORM 参数化查询，避免 SQL 注入；
   - 对评论与文章内容进行长度限制；
   - Markdown 渲染需考虑 XSS（建议后端/前端做 HTML 白名单/消毒）。
4. 上传安全：
   - 限制文件类型（如 png/jpg/webp/gif）与文件大小（如 ≤ 5MB）；
   - 文件名随机化；禁止路径穿越；
   - 可选：图片压缩与 EXIF 清理。

### 4.3 可维护性
1. 分层结构清晰：API 层、业务服务层、数据访问层分离。
2. 关键模块具备单元测试（如文章服务、鉴权、评论服务）。
3. 统一错误码与错误响应结构（便于前端处理）。
4. 提供 API 文档（FastAPI OpenAPI 自动生成）与基础开发说明（README）。

### 4.4 可用性与体验
1. 前台适配移动端与桌面端（响应式布局）。
2. 后台表单编辑体验良好（Markdown 编辑器、标签输入、图片上传）。
3. 空态/错误态提示清晰。

### 4.5 可扩展性
1. 数据模型预留扩展字段（如文章阅读数、点赞数、SEO 字段）。
2. 未来可替换数据库（SQLite → PostgreSQL/MySQL）时尽量少改动业务逻辑（使用 SQLAlchemy 等 ORM）。

---

## 5. 数据实体初步描述
说明：为覆盖“评论、标签、草稿、图片上传”等需求，在原始的“文章、分类、用户”基础上补充必要实体。若你坚持最小实体，也可将标签简化为文章表的字符串字段，但不利于筛选与一致性维护。

### 5.1 用户（User）
**用途**：后台管理员登录（本期可仅有管理员）。  
**核心字段建议**
- id（主键）
- username（唯一）
- password_hash
- is_active
- created_at / updated_at

### 5.2 分类（Category）
**用途**：文章分类筛选。  
**核心字段建议**
- id
- name（唯一）
- slug（可选，用于 URL 友好）
- created_at / updated_at

### 5.3 标签（Tag）
**用途**：文章标签筛选。  
**核心字段建议**
- id
- name（唯一）
- slug（可选）

> 关系：Article 与 Tag 多对多（建议中间表 article_tags）。

### 5.4 文章（Article）
**用途**：核心内容实体。  
**核心字段建议**
- id
- title（必填）
- summary（可选）
- content_md（Markdown 原文，必填）
- content_html（可选，预渲染缓存）
- cover_image_url（可选）
- status（枚举：draft / published；用户提出）
- category_id（外键）
- published_at（发布时刻；草稿可为空）
- created_at / updated_at

> 标签字段实现建议（两种方案）：  
> - **推荐**：独立 Tag 表 + 中间表（便于筛选与去重）。  
> - 简化：Article.tags 作为逗号分隔字符串（实现快但维护成本高）。

### 5.5 评论（Comment）
**用途**：文章互动（用户提出）。  
**核心字段建议**
- id
- article_id（外键）
- author_name（匿名昵称）
- author_email（可选）
- content（必填）
- status（可选：visible/hidden 或 is_deleted）
- created_at / updated_at

### 5.6 图片资源（Media）（可选但推荐）
**用途**：图片上传的落库管理（便于追踪与清理）。  
**核心字段建议**
- id
- url / path
- mime_type
- size
- width / height（可选）
- created_at
- ref_article_id（可选，引用追踪）

---

## 6. 技术栈选型与理由

### 6.1 后端：Python + FastAPI
**理由**
1. FastAPI 基于类型注解，接口定义清晰，自动生成 OpenAPI/Swagger 文档，利于前后端协作与验收。
2. 性能与开发效率平衡良好，适合中小型内容系统快速落地。
3. 生态成熟：可搭配 SQLAlchemy、Pydantic、Alembic、pytest 等形成完整工程化体系。

### 6.2 前端：Vue 3
**理由**
1. Composition API + TypeScript 友好，适合构建后台管理与前台页面。
2. 生态完善：路由（vue-router）、状态管理（pinia）、UI 框架（Element Plus 等）可快速搭建后台。
3. 与 Markdown 编辑器、代码高亮、富文本渲染等技术整合成本低。

### 6.3 数据库：SQLite
**理由**
1. 零运维、单文件数据库，适合个人博客与轻量部署。
2. 与 Python 集成简单，易于本地开发与备份迁移。
3. 后续可通过 ORM 平滑迁移到 PostgreSQL/MySQL（非本期，但架构建议预留）。

### 6.4 关键配套建议（可选，但强烈推荐）
1. ORM：SQLAlchemy 2.x（或 SQLModel）——模型清晰、迁移可控。  
2. 迁移：Alembic —— 数据库版本可管理。  
3. 鉴权：JWT（fastapi-users 或自研轻量实现）——后台简单可控。  
4. 前端 UI：Element Plus —— 后台表格表单效率高。  
5. Markdown：
   - 编辑：md-editor-v3 / @toast-ui/editor（二选一）
   - 展示：markdown-it + highlight.js，并做 XSS 防护（DOMPurify 等）

---

## 7. 验收与里程碑建议（可选）
1. M1：前台列表/详情 + 后台登录 + 文章 CRUD + 发布/草稿（满足核心闭环）
2. M2：分类/标签筛选完善 + 评论（前台发表 + 后台管理）
3. M3：图片上传完善（校验、压缩、资源管理）+ 体验优化

